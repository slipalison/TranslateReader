D-2026-08-01-translated-epub-images-1: Phase 'Imagem quebrada em livro traduzido' (slug: translated-epub-images) added. Reason: card despachado pelo usuario via `/jdi-issue` em 2026-08-01 — "preciso que voce analise com mais pontos de vista o problema das imagens em livros traduzidos, algumas imagens estao quebradas, isso nao pode ocorrer. Muitos livros traduzidos estao com problema assim que e aberto da biblioteca, mostra uma imagem quebrada" (texto colado, sem URL de tracker). Evidencia coletada em `main` (`9e07c83`) no momento do registro, nao hipotese: (1) `ParsingEngine.ReplaceImageRef` monta `var bookDir = Path.GetFileName(imagesDirectory); var imageUrl = $"https://epub-images/{bookDir}/{resolvedPath}"` — URL do virtual host que so existe no mapeamento do WebView do app; (2) `RewriteImagePaths` aplica isso a `<img src>`, `<image xlink:href>` e `<image href>` de TODO capitulo devolvido por `ExtractChapterContentAsync`; (3) `TranslationManager` chama `ExtractChapterContentAsync(book.FilePath, href, string.Empty)` nas linhas 123, 193 e 242 — `imagesDirectory` VAZIO, entao `bookDir` fica vazio e a URL sai como `https://epub-images//OEBPS/...`; (4) a linha 193 e o caminho da traducao de livro completo, cujo HTML e passado a `ParsingEngine.CreateTranslatedEpubAsync`, que copia o EPUB original e SOBRESCREVE as entradas de capitulo com esse HTML. Consequencia: o EPUB traduzido e gravado com `<img src="https://epub-images//...">`, que nao resolve ao abrir o livro traduzido pela biblioteca (o app extrai as imagens para o diretorio do livro NOVO, e a URL gravada aponta para diretorio vazio/do livro original) nem em qualquer leitor de EPUB de terceiro. O card pede explicitamente analise com MAIS PONTOS DE VISTA, entao a fase nao pode parar na primeira causa: precisa cobrir tambem capa/`CoverImagePath` na biblioteca, `InlineCssLinks` (que igualmente muta o HTML gravado no artefato), o caminho de reimportacao do EPUB traduzido em `LibraryPageModel`, e o que fazer com os livros ja gerados quebrados.
