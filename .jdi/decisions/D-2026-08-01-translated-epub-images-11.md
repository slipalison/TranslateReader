D-2026-08-01-translated-epub-images-11 (correcao de medicao, nao muda comando nem criterio): a `D-2026-08-01-translated-epub-images-10` registra, no cenario (b) "ref `main` local AUSENTE (clone fresco de CI)", que o `Verify:` ANTIGO do item 5 saia com "exit 0 OCO". A medicao independente do reviewer na re-verify da iter 2 nao reproduz isso: o comando antigo sai **exit 1** naquele cenario. A derivacao degrada oca como descrito (`B=0`, `S=0`, 0 nomes na base) e emite 3x `fatal` no stderr, mas o `awk` final reprova em `Skipped(2) <= S(0)` — ou seja, o gate antigo falhava FECHADO por acidente, e o comportamento oco era condicional a uma suite sem testes `Skip`. A motivacao de `D-...-10` continua valida e foi verificada de forma independente pelos outros cenarios (ref local atrasado: antigo reprova codigo correto; base avancando com o PR aberto: antigo reprova; ref ausente: antigo quebra com `fatal: bad revision 'main'`), e os comandos novos foram medidos exit 0 no repo real e reprovando as 4 mutacoes de regressao de gate. Nada a mudar em codigo, criterio ou comando — este registro existe so para o ledger nao carregar uma medicao errada, mesmo tratamento que a `-10` deu a imprecisao de prosa da `-9`.
