# Phase 21: Redesign visual do app (Library, Reader, Settings) — Summary (slug: app-redesign)

## Executed tasks
- T-4: `Border` externo (`PanelBorder`, novo `x:Name`) ganhou `OnIdiom` SO em `HorizontalOptions`/`VerticalOptions`/`WidthRequest`/`Padding` + `RoundRectangle.CornerRadius` (Desktop = painel lateral direito, altura cheia, sem raio; default = bottom sheet de hoje, raio `16,16,0,0`) — nenhum `SettingsPanel.xaml`/`SettingsSheet.xaml` criado, nenhum controle irmao duplicado, conteudo interno identico nos 2 idioms. `ShowAsync()`/`HideAsync()` novos no code-behind (contrato congelado do PLAN) usam `FadeToAsync`/`TranslateToAsync` (backdrop com `x:Name="Backdrop"` funde, painel desliza a partir de `DeviceDisplay.MainDisplayInfo` em DIPs — evita depender de medida de layout de um controle que comeca `IsVisible=False`); substituem o flip direto de `IsVisible` que o `SyncSettingsOverlay` do `ReaderPage.xaml.cs` faz hoje (T-7 vai trocar a chamada). Os 18 `x:Name` de hoje e os 3 eventos publicos (`CloseRequested`, `SettingsChanged`, `DeleteModelRequested`) preservados; atribuicao "Powered by Tencent HY" preservada exatamente. Hex legados do `.xaml` (bg/texto do painel, divisor, chips de modo/modelo, borda de selecao, texto mudo) e os 9 `Color.FromArgb` do `.xaml.cs` (agora um unico `SelectedIndicatorColor` resolvido de `ColorAccent` via `Application.Current.Resources`) viram `{StaticResource ...}` de `DesignTokens.xaml` (`ColorSurface`, `ColorText`, `ColorDivider`, `ColorAccent`, `Neutral800/700/500/400`); amostras dos temas de leitura mantidas como conteudo (`#FFFFFF`/`#1A1A1A`, `#F4ECD8`/`#5B4636`) e `#E53E3E` (acao destrutiva) preservados. Secoes "Tema"/"Modo de leitura"/"Traducao" e sliders ganharam estilo consistente com os tokens (uppercase+tracking, trilha/thumb `ColorAccent`) sem novo `OnIdiom`. Sem teste de unidade possivel (`ContentView` fora do alcance do harness, D-...-10) — coberto por `dotnet build -f net10.0-windows10.0.19041.0` isolado: 0 erro/warning originado dos 2 arquivos permitidos (os 2 `CS1503` remanescentes em `LibraryPageModel.cs:126,140` sao a mesma quebra pre-existente ja documentada na entrada de T-5, resolvida por T-6). `dotnet test`: 354 passed/2 skipped, sem regressao.
- T-5: `TranslateBookPopup` ctor virou `public TranslateBookPopup(BookSummary book)` (era `string bookTitle`); `x:Name="BookMetaLabel"` (novo, substitui `BookTitleLabel`) mostra titulo em negrito + `"Autor · N capitulos"` via `FormattedString` (autor omitido quando vazio); `x:Name="OfflineBanner"` novo com o aviso "A traducao roda 100% offline com o modelo local. Nenhum texto sai do seu dispositivo."; icone de capa placeholder com gradiente `ColorSection`/`ColorSectionGlow`; os 7 hex legados (`#FFFFFF`, `#1A1A2E`, `#E0E0E0`, `#333333`, `#666666`/`#999999`, `#F0F0F0`/`#2A2A3E`, `#1A1A1A`/`#E4E4E7`, `#2563EB`) removidos e substituidos por `DesignTokens` (`ColorSurface`, `Neutral500/800/400/100`, `ColorDivider`, `Accent900/700/200`, `ColorText`, `ColorAccent`), sem `AppThemeBinding` (chrome dark-only); resultado do popup continua `(string source, string target)`; `SourcePicker`/`TargetPicker` inalterados no comportamento. Sem teste de unidade possivel (`ContentView` fora do alcance do harness, D-...-10) — coberto por `dotnet build -f net10.0-windows10.0.19041.0` isolado nos 2 arquivos permitidos: build limpo, sem erro/warning originado deles. **Nota:** o build completo do app HOJE falha com `CS1503` em `LibraryPageModel.cs:126,140` (`new TranslateBookPopup(book.Title)` ainda passa `string`) — e o contrato congelado esperado (PLAN.md linha 24/87): T-6 atualiza os 2 call sites para `new TranslateBookPopup(book)`. `dotnet test` (suite Core) segue verde: 354 passed/2 skipped, sem regressao (o projeto de teste nao referencia `TranslateReader`, so `TranslateReader.Core`).
- T-3: `GetSelectedModelStatusAsync()` adicionado a `ITranslationManager`/`TranslationManager`, reusando `ResolveModel` + `IModelAccess.IsModelAvailable`; novo `TranslationModelStatus` record; 3 testes novos (`GetSelectedModelStatusAsync_ReturnsTheModelSelectedInSettings`, `GetSelectedModelStatusAsync_ReportsDownloadedWhenTheFileExists`, `GetSelectedModelStatusAsync_FallsBackToGemmaForAnUnregisteredName`); 43 testes legados de `TranslationManagerTests` intactos (46 total, 0 falhas); `line-rate=1.0` em `TranslationManager`.
- T-2: `BookSummary` ganha `LastReadAt`/`TotalChapters`; `ILibraryManager.ListBookSummariesAsync(string query = "")` com filtro inline titulo/autor (`OrdinalIgnoreCase`, precedente do `SearchBooksAsync`) e `ListRecentBookSummariesAsync()` (ordenado desc, exclui livro sem `ReadingProgress`); `LastReadAt` projetado de `ReadingProgress.UpdatedAt` (nao de `Book.LastOpenedAt`); `FetchAllBooksAsync` chamado 1x por operacao; 5 testes novos + teste legado `ListBookSummariesAsync_ReturnsSummaryWithProgress` intacto (14 total em `LibraryManagerTests`, 0 falhas); `line-rate=1.0` em `LibraryManager`.
- T-1: novo `DesignTokens.xaml` com os 9 hexes exigidos + escalas neutra/accent/accent-2 + radius + shadow (hairline stroke + drop shadow) de `DESIGN-REFERENCE.md`; merged em `App.xaml` antes de `Colors.xaml`/`Styles.xaml`; `UserAppTheme = AppTheme.Dark` fixado em `App.xaml.cs`; `AppShell.xaml` e o `Style TargetType="Shell"` global em `Styles.xaml` restilizados por chave de token (fora o legado Gray/Primary/AppThemeBinding); `AppShell.xaml` continua com exatamente 1 `<ShellContent>`, nenhuma rota nova; `Colors.xaml` intocado (ainda usado por paginas fora de escopo, ex. BookDetailPage); `ThemeEngine`/CSS de leitura do WebView intocados; build Windows Release 0 erros, 354 passed/2 skipped (suite completa, sem regressao).
