# Phase 20: Review  (slug: hy-mt-translation-model)

**Verdict:** APPROVED_WITH_WARNINGS

## Gates
| Gate | Status | Details |
|---|---|---|
| Build | PASS | `dotnet restore` + `dotnet build src/TranslateReader/TranslateReader.csproj -c Release -f net10.0-windows10.0.19041.0` — exit 0, `0 Erro(s)`, 40 warnings (all pre-existing MVVMTK0045/XC0022-class, none new to this phase). Android/iOS not built: phase did not touch `Platforms/`. |
| Tests | PASS | Full suite `-c Release`: **Failed: 0, Passed: 346, Skipped: 2, Total: 348**. Pre-phase baseline derived from `git merge-base origin/main HEAD` = 343 ([Fact]+[InlineData]); 348 = 343 + 5 new tests. The 2 skips are the pre-existing real-GGUF-gated `TranslationEngineTests`. No regression; name-by-name diff vs base empty (`comm -23`). |
| Coverage | PASS | Cobertura report `TestResults/b3e8bec4-.../coverage.cobertura.xml` (aggregate line-rate 0.9331, context only). New .cs files since boundary `4285f25` present in report: `BookTranslationResult.cs` (1.0), `ExtractedImage.cs` (1.0); `ChapterContentPurpose.cs` has no coverable lines (enum). Gate average over new files = **100% >= 90%**. Doer's D-6 claim on changed code independently verified from the XML, not trusted from SUMMARY: `ModelAccess` class 100% line/100% branch; `TranslationManager` class + `<DownloadModelIfNeededAsync>d__15` + `<InitializeEngineIfNeededAsync>d__16` all 1.0/1.0 (`ResolveModel` folds into the class entry, 1.0/1.0). The only sub-1.0 branch entries (`TranslateChapterAsync`/`TranslateParagraphsAsync` at 0.833) are legacy methods untouched by this phase. |
| Lint | PASS | `dotnet format --verify-no-changes` exit 0 — no format drift at all (gate is WARN-only, but it came back clean). |
| Security/Layer | PASS (warnings) | 5.1 client->Access/Engines: clean. 5.2 storage tech in Contracts/Access: clean. 5.3 Manager->Manager: clean — every hit is a Manager implementing its OWN interface; `TranslationManager` ctor injects `ISettingsAccess` (ResourceAccess contract), NOT `ISettingsManager`, exactly as D-...-4 locked. 5.5: `ResolveModel` = `TryGetValue` + explicit gemma fallback with WHY comment — judged orchestration-adjacent lookup per locked D-...-4, not a business rule smuggled into the Manager. 5.9/5.10/5.15b/5.17a: clean. 5.11: no new `+=` in the phase diff — `OnHyMtClicked` is XAML-wired (`Clicked=`), same lifecycle as the 3 existing buttons; baseline imbalance not re-flagged. 5.12: `ModelRegistry` is `private static readonly IReadOnlyDictionary<string, ModelInfo>` with `StringComparer.Ordinal` — compliant; only static hit is the known legacy `TranslationEngine._nativeLibraryConfigured` baseline (not re-flagged). 5.13: registry is a fixed 2-entry compile-time table, not a cache — not flagged per spec. 5.15: `GetModelPath` throws `FileNotFoundException` with no user-profile path in the message — fail-fast honored. 5.16: both changed `IModelAccess` members carry 1-line `<summary>`; no TODO/commented-out code in the diff; changed methods all well under 20 lines; 8-param ctor -> W1. 5.17b: the 2 new `ModelAccessTests` use the identical isolated temp-dir (`Path.GetTempPath()` + Guid) fixture the file already had pre-phase — not a new violation. Legacy-only findings -> W2/W3. |
| Consistency | PASS | All 8 PLAN files_modified appear in the 5 task commits (`167f21d`, `ea13ec6`, `882107b`, `9c9a09d`, `4f5d90c`); commit messages match PLAN verbatim, conventional-commit types are semantically correct (test/docs/feat/fix, not blindly feat), scope = phase slug on all. T-6 intentionally has no commit (no versioned diff), matching PLAN. RED-first transcripts for T-1 and T-5 present in SUMMARY per csharp.md §6. |
| UI Validation | SKIPPED | has_frontend=false |
| DoD | PASS | 6/6 auto, 0 manual — every `Verify:` run VERBATIM from CONTEXT.md, all exit 0 (table below). `.jdi/PROJECT.md` has no `## Definition of Done` section (confirmed by grep), so only the phase DoD applies. |

## Blockers
None.

## Warnings
1. **W1 — 8-parameter `TranslationManager` primary constructor** (`src/TranslateReader.Core/Business/Managers/TranslationManager.cs:13-21`), one over the csharp.md §7 / Sonar S107 ceiling of 7. KNOWN, ACCEPTED, DOCUMENTED exposure forced by locked decision D-...-4 + the DoD 6 exact-diff-scope gate (a parameter object would need a new Core file or a `MauiProgram.cs` change, both prohibited). Reported for PR-review visibility only — explicitly NOT a blocker, and the locked decision forbade refactoring around it.
2. **W2 — legacy fail-fast drift (pre-existing, untouched this phase, adopted=true -> non-blocking):** `catch { }` at `src/TranslateReader/Pages/ReaderPage.xaml.cs:326` and `:434`; swallowed `OperationCanceledException` at `src/TranslateReader/PageModels/LibraryPageModel.cs:183`, `src/TranslateReader/PageModels/ReaderPageModel.cs:222`, `src/TranslateReader/Pages/ReaderPage.xaml.cs:308` (csharp.md §1 says OCE always flows). All pre-date the phase (DoD 6 proves the app-side diff is limited to `SettingsOverlay.xaml(.cs)`).
3. **W3 — legacy PageModels inject >1 Manager** (`LibraryPageModel` 2, `ReaderPageModel` 3 — vs CLAUDE.md rule "max 1 Manager per use case"). Pre-existing, not touched by this phase.
4. **W4 — DoD critic finding on row 4 (license visibility):** the attribution label + 4th model button sit in `SettingsOverlay`'s non-scrolling-vertical bottom sheet (`VerticalStackLayout` inside a bottom-anchored `Border`, no vertical `ScrollView`); ~13 stacked sections may exceed a phone viewport height, so "reachable by the user" is unverified beyond static reading — no grep/build can prove runtime layout. Not objectively disproven (suspicion only), so not a blocker; flagged for the on-device smoke test already listed in CONTEXT's Deferred to PR review.

## DoD Checklist (gate 8)
| # | Criterion | Source | Type | Status | Evidence |
|---|---|---|---|---|---|
| 1 | Registry has both real entries with session-validated values; wrong card number absent; typed dictionary | CONTEXT (D-...-2, D-...-4) | Auto | PASS | Verbatim grep chain over `TranslationManager.cs` exit 0: `Name: "hy-mt1.5-1.8b"`, `FileName: "HY-MT1.5-1.8B-Q4_K_M.gguf"`, exact resolve URL, `SizeBytes: 1_133_080_512`, `Name: "gemma-2-2b"`, `SizeBytes: 1_629_413_888`, zero `1_213_000_000`, `IReadOnlyDictionary<string, ModelInfo>` matched |
| 2 | Settings-driven resolution real: hy-mt selected uses hy-mt URL/file, unknown name falls back to gemma; 7 filter tests pass | CONTEXT (D-...-4) | Auto | PASS | Verbatim command exit 0 — `TestResults/dod2.log`: `Passed! - Failed: 0, Passed: 7, Total: 7` (floor n=7 met) |
| 3 | `IsModelAvailable`/`GetModelPath` filename-aware; 8 filter tests incl. the 2 foreign-gguf difference tests | CONTEXT (D-...-4) | Auto | PASS | Verbatim command exit 0 — contract signatures matched in `IModelAccess.cs`; `TestResults/dod3.log`: `Passed! - Failed: 0, Passed: 8, Total: 8` (floor n=8 met) |
| 4 | License visible and reachable: `THIRD-PARTY-NOTICES.md` with real clauses; 4th button + attribution in `SettingsOverlay`; app builds | CONTEXT (D-...-3) | Auto | PASS | Verbatim command exit 0 — all 6 notice strings + `HyMtModelButton` + `OnHyMtClicked` + `"hy-mt1.5-1.8b"` matched; `TestResults/dod4.log`: `0 Error(s)` (8 warnings, pre-existing). Reachability honored beyond the grep: buttons wrapped in horizontal `ScrollView` (R5) |
| 5 | Full suite no regression: Failed 0, Total >= B+5, Skipped <= S, sum coherent, no base test name missing | CONTEXT | Auto | PASS | Verbatim command exit 0 — B=343, S=2, floor=348; `TestResults/dod5.log`: `Failed: 0, Passed: 346, Skipped: 2, Total: 348`; `comm -23 base head` empty |
| 6 | Diff scope closed: Engine/PromptUtility/ITranslationManager/ModelInfo untouched; app diff = SettingsOverlay only; Core diff = exactly 3 files; notices file exists | CONTEXT (D-...-4/5/6) | Auto | PASS | Verbatim command exit 0 — Core diff is exactly `Access/ModelAccess.cs,Business/Managers/TranslationManager.cs,Contracts/Access/IModelAccess.cs,`; excluded-file diffs empty |

**Totals:** 6/6 auto PASS, 0 manual pending, 0 FAIL.

## DoD Critic (enhanced)
- DoD row «4» (licenca visivel e alcancavel): hollow=true, objective=false. Todo sub-criterio automatizavel (clausulas reais em `THIRD-PARTY-NOTICES.md`, botao/atribuicao presentes no XAML, `OnHyMtClicked` funcional, build 0 erros) esta genuinamente provado. Mas a clausula "alcancavel pelo usuario" e um requisito de layout em runtime que nenhum `grep`/build prova: o `ScrollView` horizontal (`SettingsOverlay.xaml:192`) cobre so o overflow horizontal da fileira de botoes; a folha de settings inteira e um `VerticalStackLayout` (`:24`) dentro de um `Border` ancorado embaixo (`:15`), sem scroll vertical, com ~13 secoes empilhadas que plausivelmente excedem a altura de viewport de um celular. Mutantes contra-exemplo (`IsVisible="False"` no botao, corte por viewport) passariam pelos greps e pelo build sem detectar. Leitura estatica sugere que os elementos novos ficam na parte mais visivel (fundo da folha), entao a suspeita nao e objetivamente demonstravel sem rodar o app num device — por isso nao vira BLOCKED, so fica registrada.

**Verdict:** APPROVED_WITH_WARNINGS

## Recommendation
Ship it. All blocking gates green, all 6 DoD `Verify:` commands pass run verbatim, coverage on new/changed code independently confirmed at 100% from the Cobertura XML, and every locked decision (ISettingsAccess not ISettingsManager, static readonly Ordinal registry, explicit gemma fallback, untouched Engine/PromptUtility/ModelInfo, no PromptTemplate field) is honored in the code as written. W1 is intentional, decision-forced debt — the PR reviewer should either accept it or schedule a follow-up phase; W2/W3 are legacy-only observations for a future cleanup phase, not this one. The CONTEXT "Deferred to PR review" items remain live for the human reviewer: EU/UK/South Korea residual license risk (no geo-gating, D-...-3), hy-mt running at uniform Temperature=0.1 vs vendor-recommended 0.7 (D-...-5), no on-device download/translation smoke test, SonarCloud pending push+CI, and legal wording of the notices/attribution unreviewed by counsel.
